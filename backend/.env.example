# Google Sheets Expense Tracker Environment Variables
PORT=8000
SPREADSHEET_ID=
CREDENTIALS_FILE=credentials.json
SPREADSHEET_NAME=Personal Expense Tracker
# Optional: Set to false to disable local JSON fallback if Google Sheet is unreachable
ENABLE_LOCAL_FALLBACK=true
